
class Config:
    main_window_dimensions = (1920, 1280)  # the width and height of the main window
    interface_frame_height = 70  # the width of the left panel of UI elements
    image_dimensions = (1920, 1080)
    num_image_previews = (8, 5)
    comment_image_dimensions = (1366, 768)
