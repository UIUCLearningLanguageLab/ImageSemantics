
class Config:
    main_window_dimensions = (1750, 1000)  # the width and height of the main window
    small_image_dimensions = (176, 99)  # the dimensions of the preview images
    large_image_scale = 4.76  # how much bigger the big image is than the smaller ones
    interface_frame_width = 160  # the width of the left panel of UI elements
    big_image_y_spacing = 10  # the amount of space between the two big images on the right
    num_image_rows = 9  # the number of preview image rows
    num_image_columns = 4  # the number of preview image columns
